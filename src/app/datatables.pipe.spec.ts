import { DatatablesPipe } from './datatables.pipe';

describe('DatatablesPipe', () => {
  it('create an instance', () => {
    const pipe = new DatatablesPipe();
    expect(pipe).toBeTruthy();
  });
});
